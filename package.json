{
  "name": "clean-the-mess",
  "version": "1.0.0",
  "description": "Hundreds of opened tabs? Dozens of useless duplicate tabs with opened emails, google searches, your favourite social networks and articles you wanted to read a month ago? Let's clean the mess!",
  "main": "src/popup/popup.js",
  "scripts": {
    "start": "npm run chrome && npm run firefox",
    "test": "jest",
    "firefox": "npx web-ext run -s ./dist/firefox",
    "chrome": "npx web-ext run -t chromium -s ./dist/chrome",
    "firefox:src": "npx web-ext run -s ./src",
    "chrome:src": "npx web-ext run -t chromium -s ./src",
    "firefox:build": "npx web-ext build -a ./build/firefox",
    "chrome:build": "npx web-ext build -a ./build/chrome"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tomas-hartman/clean-the-mess.git"
  },
  "keywords": [
    "addon",
    "mozilla",
    "tabs"
  ],
  "author": "Tomas Hartman",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/tomas-hartman/clean-the-mess/issues"
  },
  "homepage": "https://github.com/tomas-hartman/clean-the-mess#readme",
  "devDependencies": {
    "@babel/core": "^7.12.3",
    "@babel/preset-env": "^7.12.1",
    "babel-jest": "^26.6.1",
    "connect-livereload": "^0.6.1",
    "eslint": "^7.12.1",
    "eslint-plugin-jest": "^24.1.0",
    "gulp": "^4.0.2",
    "gulp-sass": "^4.1.0",
    "jest": "^26.6.1",
    "livereload": "^0.9.1",
    "node-sass": "^5.0.0",
    "web-ext": "^6.1.0",
    "webextension-polyfill": "^0.8.0"
  },
  "dependencies": {
    "express": "^4.17.1",
    "gulp-merge-json": "^2.1.1"
  }
}
